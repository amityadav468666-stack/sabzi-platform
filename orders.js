import {auth,db} from './firebase.js'; import {addDoc,collection,serverTimestamp} from 'https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js';
const form=document.querySelector('#checkoutForm'); if(form)form.addEventListener('submit',async e=>{e.preventDefault();const u=auth.currentUser;if(!u){location.href='login.html';return}const items=JSON.parse(localStorage.getItem('sabziCart')||'[]');if(!items.length)return;const total=items.reduce((s,p)=>s+p.price*p.qty,0);try{await addDoc(collection(db,'orders'),{userId:u.uid,items,total,address:address.value,paymentMethod:paymentMethod.value,status:'Pending',createdAt:serverTimestamp()});localStorage.removeItem('sabziCart');alert('ऑर्डर सफल');location.href='orders.html'}catch(x){alert(x.message)}});
