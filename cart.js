const box=document.getElementById("cart");let c=JSON.parse(localStorage.getItem("sabzigo_cart")||"[]");
function render(){box.innerHTML=c.length?"":"<p>कार्ट खाली है।</p>";let total=0;c.forEach((p,i)=>{total+=p.price*p.qty;const d=document.createElement("div");d.className="card";d.innerHTML=`<h3>${p.name}</h3><p>₹${p.price} × ${p.qty} = ₹${p.price*p.qty}</p><button class="btn">−</button> <button class="btn">+</button> <button class="btn">हटाएँ</button>`;let b=d.querySelectorAll("button");b[0].onclick=()=>change(i,-1);b[1].onclick=()=>change(i,1);b[2].onclick=()=>{c.splice(i,1);save()};box.appendChild(d)});if(c.length){const t=document.createElement("h2");t.textContent="कुल: ₹"+total;box.appendChild(t)}}
function change(i,n){c[i].qty+=n;if(c[i].qty<=0)c.splice(i,1);save()}function save(){localStorage.setItem("sabzigo_cart",JSON.stringify(c));render()}render();
