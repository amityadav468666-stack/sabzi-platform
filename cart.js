const get=()=>JSON.parse(localStorage.getItem('sabziCart')||'[]'); const save=c=>localStorage.setItem('sabziCart',JSON.stringify(c));
const box=document.querySelector('#cart'); const total=document.querySelector('#total'); function render(){const c=get();if(box)box.innerHTML=c.length?c.map((p,i)=>`<div class="card row"><span>${p.name} × ${p.qty}</span><b>₹${p.price*p.qty}</b><button class="danger" onclick="removeItem(${i})">हटाएँ</button></div>`).join(''):'<p>कार्ट खाली है।</p>';if(total)total.textContent='₹'+c.reduce((s,p)=>s+p.price*p.qty,0)} window.removeItem=i=>{const c=get();c.splice(i,1);save(c);render()};render();
